VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Web Push. Public half of the VAPID keypair — safe to ship in the bundle.
# The private half lives ONLY in Supabase Edge Function secrets, never here.
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
